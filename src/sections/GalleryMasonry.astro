---
const ASPECTS = [
    'aspect-[3/4]', 'aspect-square', 'aspect-[4/3]', 'aspect-video', 'aspect-square',
    'aspect-[3/4]', 'aspect-[4/3]', 'aspect-square', 'aspect-[3/4]', 'aspect-[4/3]',
    'aspect-square', 'aspect-video', 'aspect-square', 'aspect-[3/4]', 'aspect-[4/3]',
];
---

<section
    class="section"
    aria-labelledby="masonry-title"
>
    <div class="container">
        <h2
            id="masonry-title"
            class="sr-only"
        >
            Photo gallery
        </h2>
        <ul
            class="masonry list-none"
            data-reveal
        >
            {ASPECTS.map(aspect => (
                <li class:list={['placeholder', aspect]}><span>photo</span></li>
            ))}
        </ul>
    </div>
</section>

<style>
    .masonry {
        column-gap: 1.1rem;
        columns: 3 16rem;
    }

    .masonry .placeholder {
        break-inside: avoid;
        margin-bottom: 1.1rem;
    }
</style>
